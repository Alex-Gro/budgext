import { LoggerMiddleware } from './logger.middleware';

// TODO Remove or do correctly
describe('LoggerMiddleware', () => {
  it('should be defined', () => {
    expect(new LoggerMiddleware()).toBeDefined();
  });
});
