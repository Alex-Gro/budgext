import { TransactionsSumPipe } from './transactions-sum.pipe';

describe('TransactionsSumPipe', () => {
  it('create an instance', () => {
    const pipe = new TransactionsSumPipe();
    expect(pipe).toBeTruthy();
  });
});
